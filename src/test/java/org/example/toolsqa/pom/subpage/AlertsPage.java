package org.example.toolsqa.pom.subpage;

import org.openqa.selenium.WebDriver;

public class AlertsPage extends Subpage {
    public AlertsPage(WebDriver webDriver){
        super(webDriver);
    }
}
